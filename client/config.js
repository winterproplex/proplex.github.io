// upload.config.server = '' // Empty if the webapp is in the same place as the server
// upload.config.server = '' // If the webapp is separated from the server - remember the trailing slash
// upload.config.api_key = '' // Should be the same as the server, used for uploading
// upload.config.footer = ' |- <a href="mailto:proplexgaming@gmail.com" id="contact" target="_blank">Contact</a> -|'